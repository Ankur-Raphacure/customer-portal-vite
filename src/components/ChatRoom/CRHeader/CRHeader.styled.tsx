import styled from "styled-components";

export const CRHeaderStyled = styled.div`
  .container {
    margin: 0 1rem;
    max-width: 100%;
  }
  .dropdown-menu.show {
    left: auto;
    right: 0;
  }
  .me-auto {
    gap: 1rem;
  }
  .me-auto .nav-item {
    cursor: pointer;
  }
`;
